import Blog from '@/components/modules/Blog/Blog';
import React from 'react';

const MainPage = () => {
    return (
        <main>
            <Blog/>
        </main>
    );
};

export default MainPage;
